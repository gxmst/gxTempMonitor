<Window x:Class="TempMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TempMonitor" Width="205" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="{x:Null}"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Loaded="Window_Loaded"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseDoubleClick="Window_MouseDoubleClick"
        FontFamily="Segoe UI, Microsoft YaHei">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="Foreground" Value="#8FB0B8C3"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Cascadia Code, Cascadia Mono, Consolas, Courier New"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FFF8FBFF"/>
            <Setter Property="Width" Value="60"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="0" Color="#E0000000" Opacity="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MaxStyle" TargetType="TextBlock" BasedOn="{StaticResource ValueStyle}">
            <Setter Property="Foreground" Value="#FFE1B84A"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>

        <Style x:Key="IndicatorStyle" TargetType="Border">
            <Setter Property="Width" Value="2"/>
            <Setter Property="CornerRadius" Value="1"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Background" Value="#FFFF7448"/>
            <Setter Property="Margin" Value="-4,1,0,1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Color="#B0FF7448" Opacity="1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.ContextMenu>
        <ContextMenu x:Name="MainContextMenu">
            <MenuItem x:Name="LockMenuItem" Header="锁定 (鼠标穿透)" Click="Lock_Click"/>
            <Separator/>
            <MenuItem x:Name="StartupMenuItem" Header="开机自启" Click="Startup_Click"/>
            <Separator/>
            <MenuItem Header="显示项目">
                <MenuItem x:Name="ShowRamMenuItem" Header="显示内存 (RAM)" IsCheckable="True" Click="ShowRam_Click"/>
                <MenuItem x:Name="ShowVramMenuItem" Header="显示显存 (VRAM)" IsCheckable="True" Click="ShowVram_Click"/>
                <MenuItem x:Name="ShowUpMenuItem" Header="显示上传 (UP)" IsCheckable="True" Click="ShowUp_Click"/>
                <MenuItem x:Name="ShowDownMenuItem" Header="显示下载 (DN)" IsCheckable="True" Click="ShowDown_Click"/>
            </MenuItem>
            <MenuItem Header="恢复默认状态" Click="RestoreDefaultState_Click"/>
            <Separator/>
            <MenuItem Header="重置最大值" Click="ResetMax_Click"/>
            <MenuItem Header="退出 (Exit)" Click="Exit_Click"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Border x:Name="MainBorder"
            BorderBrush="#24F2F6FF" BorderThickness="1" CornerRadius="7"
            Padding="9,5" HorizontalAlignment="Right" Width="135"
            MouseEnter="MainBorder_MouseEnter" MouseLeave="MainBorder_MouseLeave">
        <Border.Background>
            <SolidColorBrush x:Name="MainBackgroundBrush" Color="#96101214"/>
        </Border.Background>
        <Grid>
            <StackPanel x:Name="MainStack">
                <Grid x:Name="HeaderGrid" Height="11" Opacity="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="2" Text="MAX" Foreground="#D8E1B84A" FontSize="8" FontWeight="Bold" Margin="10,0,0,0"/>
                </Grid>

                <Grid Margin="0,1" x:Name="CpuRow" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition x:Name="MaxColWidth" Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="CpuIndicator" Style="{StaticResource IndicatorStyle}"/>
                    <TextBlock Text="CPU" Style="{StaticResource LabelStyle}" IsHitTestVisible="False"/>
                    <TextBlock x:Name="CpuUsageText" Grid.Column="1" Text="0.0 %" Style="{StaticResource ValueStyle}" IsHitTestVisible="False"/>
                    <Border Grid.Column="2" x:Name="MaxContainer" Width="0" ClipToBounds="True" IsHitTestVisible="False">
                        <TextBlock x:Name="CpuMaxText" Text="0.0 %" Style="{StaticResource MaxStyle}"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="GpuRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="GpuIndicator" Style="{StaticResource IndicatorStyle}"/>
                    <TextBlock Text="GPU" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="GpuTempText" Grid.Column="1" Text="-- °C" Style="{StaticResource ValueStyle}"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="GpuMaxText" Text="-- °C" Style="{StaticResource MaxStyle}"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="RamRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="RamIndicator" Style="{StaticResource IndicatorStyle}"/>
                    <TextBlock Text="RAM" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="RamUsedText" Grid.Column="1" Text="0.0 GB" Style="{StaticResource ValueStyle}"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="RamMaxText" Text="0.0 GB" Style="{StaticResource MaxStyle}"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="VramRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="VRAM" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="VramUsedText" Grid.Column="1" Text="0.0 GB" Style="{StaticResource ValueStyle}"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="VramMaxText" Text="0.0 GB" Style="{StaticResource MaxStyle}"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="NetUpRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="UP ↑" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="NetUpText" Grid.Column="1" Text="0.0K" Style="{StaticResource ValueStyle}" Foreground="#FF9F6E6E" FontSize="10"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="NetUpMaxText" Text="0.0K" Style="{StaticResource MaxStyle}" FontSize="10"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="NetDownRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="DW ↓" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="NetDownText" Grid.Column="1" Text="0.0K" Style="{StaticResource ValueStyle}" Foreground="#7ED89C" FontSize="10"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="NetDownMaxText" Text="0.0K" Style="{StaticResource MaxStyle}" FontSize="10"/>
                    </Border>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>

<Window x:Class="TempMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="gxTempMonitor" Width="229" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="{x:Null}"
        Topmost="True" ShowActivated="False" ShowInTaskbar="False" ResizeMode="NoResize"
        Loaded="Window_Loaded"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseDoubleClick="Window_MouseDoubleClick"
        FontFamily="Segoe UI, Microsoft YaHei">

    <Window.Resources>
        <SolidColorBrush x:Key="ThemeLabelBrush" Color="#FFD6DEE8"/>
        <SolidColorBrush x:Key="ThemeValueBrush" Color="#FFF8FBFF"/>
        <SolidColorBrush x:Key="ThemeMaxBrush" Color="#FFFFD36B"/>
        <SolidColorBrush x:Key="ThemeIndicatorBrush" Color="#FFFF7448"/>
        <SolidColorBrush x:Key="ThemeNetUpBrush" Color="#FFFFB3A7"/>
        <SolidColorBrush x:Key="ThemeNetDownBrush" Color="#FF9FF3C6"/>
        <SolidColorBrush x:Key="ThemeBorderBrush" Color="#3AF2F6FF"/>
        <Color x:Key="ThemeFlashBaseColor">#14171C</Color>
        <Color x:Key="ThemeFlashAlertColor">#6B1414</Color>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeLabelBrush}"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="3" ShadowDepth="0" Color="#F2000000" Opacity="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Cascadia Code, Cascadia Mono, Consolas, Courier New"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeValueBrush}"/>
            <Setter Property="Width" Value="72"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="3" ShadowDepth="0" Color="#F4000000" Opacity="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MaxStyle" TargetType="TextBlock" BasedOn="{StaticResource ValueStyle}">
            <Setter Property="Foreground" Value="{DynamicResource ThemeMaxBrush}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>

        <Style x:Key="IndicatorStyle" TargetType="Border">
            <Setter Property="Width" Value="2"/>
            <Setter Property="CornerRadius" Value="1"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Background" Value="{DynamicResource ThemeIndicatorBrush}"/>
            <Setter Property="Margin" Value="-4,1,0,1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Color="#B0FF7448" Opacity="1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.ContextMenu>
        <ContextMenu x:Name="MainContextMenu">
            <MenuItem Header="打开 Dashboard" Click="Dashboard_Click"/>
            <MenuItem Header="设置..." Click="Settings_Click"/>
            <Separator/>
            <MenuItem x:Name="LockMenuItem" Header="锁定 (鼠标穿透)" Click="Lock_Click"/>
            <Separator/>
            <MenuItem x:Name="StartupMenuItem" Header="开机自启" Click="Startup_Click"/>
            <Separator/>
            <MenuItem Header="显示项目">
                <MenuItem x:Name="ShowRamMenuItem" Header="显示内存 (RAM)" IsCheckable="True" Click="ShowRam_Click"/>
                <MenuItem x:Name="ShowVramMenuItem" Header="显示显存 (VRAM)" IsCheckable="True" Click="ShowVram_Click"/>
                <MenuItem x:Name="ShowUpMenuItem" Header="显示上传 (UP)" IsCheckable="True" Click="ShowUp_Click"/>
                <MenuItem x:Name="ShowDownMenuItem" Header="显示下载 (DN)" IsCheckable="True" Click="ShowDown_Click"/>
            </MenuItem>
            <MenuItem Header="主题">
                <MenuItem x:Name="DarkThemeMenuItem" Header="深色" IsCheckable="True" Click="DarkTheme_Click"/>
                <MenuItem x:Name="LightThemeMenuItem" Header="浅色" IsCheckable="True" Click="LightTheme_Click"/>
            </MenuItem>
            <MenuItem Header="透明度">
                <MenuItem x:Name="Opacity50MenuItem" Header="50%" IsCheckable="True" Click="Opacity50_Click"/>
                <MenuItem x:Name="Opacity65MenuItem" Header="65%" IsCheckable="True" Click="Opacity65_Click"/>
                <MenuItem x:Name="Opacity80MenuItem" Header="80%" IsCheckable="True" Click="Opacity80_Click"/>
                <MenuItem x:Name="Opacity95MenuItem" Header="95%" IsCheckable="True" Click="Opacity95_Click"/>
            </MenuItem>
            <MenuItem Header="延迟启动">
                <MenuItem x:Name="DelayOffMenuItem" Header="关闭" IsCheckable="True" Click="DelayOff_Click"/>
                <MenuItem x:Name="Delay10MenuItem" Header="10 秒" IsCheckable="True" Click="Delay10_Click"/>
                <MenuItem x:Name="Delay20MenuItem" Header="20 秒" IsCheckable="True" Click="Delay20_Click"/>
                <MenuItem x:Name="Delay30MenuItem" Header="30 秒" IsCheckable="True" Click="Delay30_Click"/>
                <MenuItem x:Name="Delay60MenuItem" Header="60 秒" IsCheckable="True" Click="Delay60_Click"/>
            </MenuItem>
            <MenuItem Header="低干扰选项">
                <MenuItem x:Name="HotkeyMenuItem" Header="全局热键 Ctrl+Shift+M" IsCheckable="True" Click="Hotkey_Click"/>
                <MenuItem x:Name="ProcessTrackingMenuItem" Header="进程级 GPU 显存（全部 GPU）" IsCheckable="True" Click="ProcessTracking_Click"/>
                <MenuItem x:Name="AlertsMenuItem" Header="启用告警" IsCheckable="True" Click="Alerts_Click"/>
                <MenuItem Header="采样频率">
                    <MenuItem x:Name="Sample1MenuItem" Header="1 秒" IsCheckable="True" Click="Sample1_Click"/>
                    <MenuItem x:Name="Sample2MenuItem" Header="2 秒" IsCheckable="True" Click="Sample2_Click"/>
                    <MenuItem x:Name="Sample5MenuItem" Header="5 秒" IsCheckable="True" Click="Sample5_Click"/>
                </MenuItem>
            </MenuItem>
            <Separator/>
            <MenuItem Header="导出 CSV" Click="ExportCsv_Click"/>
            <MenuItem Header="恢复默认状态" Click="RestoreDefaultState_Click"/>
            <Separator/>
            <MenuItem Header="重置最大值" Click="ResetMax_Click"/>
            <MenuItem Header="退出 (Exit)" Click="Exit_Click"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Border x:Name="MainBorder"
            BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1" CornerRadius="7"
            Padding="9,5" HorizontalAlignment="Right" Width="147"
            MouseEnter="MainBorder_MouseEnter" MouseLeave="MainBorder_MouseLeave">
        <Border.Background>
            <SolidColorBrush x:Name="MainBackgroundBrush" Color="#B014171C"/>
        </Border.Background>
        <Grid>
            <StackPanel x:Name="MainStack">
                <Grid x:Name="HeaderGrid" Height="11" Opacity="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="72"/>
                        <ColumnDefinition Width="72"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="2" Text="MAX" Foreground="{DynamicResource ThemeMaxBrush}" FontSize="8" FontWeight="Bold" Margin="10,0,0,0"/>
                </Grid>

                <Grid Margin="0,1" x:Name="CpuRow" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="72"/>
                        <ColumnDefinition x:Name="MaxColWidth" Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="CpuIndicator" Style="{StaticResource IndicatorStyle}"/>
                    <TextBlock Text="CPU" Style="{StaticResource LabelStyle}" IsHitTestVisible="False"/>
                    <TextBlock x:Name="CpuUsageText" Grid.Column="1" Text="0.0 %" Style="{StaticResource ValueStyle}" IsHitTestVisible="False"/>
                    <Border Grid.Column="2" x:Name="MaxContainer" Width="0" ClipToBounds="True" IsHitTestVisible="False">
                        <TextBlock x:Name="CpuMaxText" Text="0.0 %" Style="{StaticResource MaxStyle}"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="GpuRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="72"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="GpuIndicator" Style="{StaticResource IndicatorStyle}"/>
                    <TextBlock Text="GPU" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="GpuTempText" Grid.Column="1" Text="-- °C" Style="{StaticResource ValueStyle}"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="GpuMaxText" Text="-- °C" Style="{StaticResource MaxStyle}"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="RamRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="72"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="RamIndicator" Style="{StaticResource IndicatorStyle}"/>
                    <TextBlock Text="RAM" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="RamUsedText" Grid.Column="1" Text="0.0 GB" Style="{StaticResource ValueStyle}"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="RamMaxText" Text="0.0 GB" Style="{StaticResource MaxStyle}"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="VramRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="72"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="VRAM" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="VramUsedText" Grid.Column="1" Text="0.0 GB" Style="{StaticResource ValueStyle}"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="VramMaxText" Text="0.0 GB" Style="{StaticResource MaxStyle}"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="NetUpRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="72"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="UP ↑" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="NetUpText" Grid.Column="1" Text="0.0K" Style="{StaticResource ValueStyle}" Foreground="{DynamicResource ThemeNetUpBrush}" FontSize="10"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="NetUpMaxText" Text="0.0K" Style="{StaticResource MaxStyle}" FontSize="10"/>
                    </Border>
                </Grid>

                <Grid Margin="0,1" x:Name="NetDownRow">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35"/>
                        <ColumnDefinition Width="72"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="DN ↓" Style="{StaticResource LabelStyle}"/>
                    <TextBlock x:Name="NetDownText" Grid.Column="1" Text="0.0K" Style="{StaticResource ValueStyle}" Foreground="{DynamicResource ThemeNetDownBrush}" FontSize="10"/>
                    <Border Grid.Column="2" Width="{Binding ElementName=MaxContainer, Path=Width}" ClipToBounds="True">
                        <TextBlock x:Name="NetDownMaxText" Text="0.0K" Style="{StaticResource MaxStyle}" FontSize="10"/>
                    </Border>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>

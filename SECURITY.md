# 安全策略

## 支持范围

安全修复优先应用到最新的 GitHub Release 和 `master` 分支。较早版本可能不会单独回补，建议先确认问题在最新版中仍可复现。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，不要在公开 issue 中附上可直接利用的细节。

报告中建议包含：

- gxTempMonitor 版本和下载来源；
- Windows 版本、系统架构和相关显卡类型；
- 问题影响与最小复现步骤；
- 文件 SHA-256，以及安全软件的具体告警名称（如适用）；
- 能够帮助确认问题的日志片段。提交前请移除用户名、完整本地路径、进程名和其他个人信息。

项目不会要求报告者上传配置文件、进程列表或其他用户数据。程序内“诊断”页生成的报告会主动省略这些信息。

## 安全边界

gxTempMonitor 以普通用户权限运行，不捆绑内核驱动，不注入或挂钩其他应用，也不读取其他进程内存。CPU 温度按设计不采集。可选的进程级 GPU 显存功能只在用户明确启用后查询 Windows 性能计数器和进程名称；其数值按 PID 汇总全部 GPU 适配器，不按当前固定 GPU 过滤。

未签名发布包会明确标注 `unsigned`。请从官方 Release 下载并核对随附的 SHA-256；代码签名状态和签名流程见 `release/SIGNING.md`。
